SELECT DISTINCT BIT_DB.customers.acctnum
FROM BIT_DB.customers
;

-- 1. How many many unique orders were placed in January? 

SELECT COUNT(DISTINCT orderID)
FROM BIT_DB.JanSales
WHERE LENGTH(orderid) = 6;

-- 2. How many of those orders were for an iPhone?

SELECT COUNT(DISTINCT JanSales.orderID)
FROM BIT_DB.JanSales
WHERE JanSales.Product = 'iPhone'
AND LENGTH(orderid) = 6;

-- 3. Select the customer account numbers for all the orders that were placed in February.

SELECT DISTINCT Customers.acctnum
FROM BIT_DB.customers AS Customers
INNER JOIN BIT_DB.FebSales AS February
ON Customers.order_id = February.orderID
WHERE LENGTH(February.orderID) = 6;

--4. Which product was the cheapest one sold in January, and what was the price?

SELECT January.Product,
MIN(January.price) AS cheapest
FROM BIT_DB.JanSales AS January
WHERE LENGTH(January.price) > 0
AND January.Product <> 'Product'
GROUP BY January.Product
ORDER BY cheapest
LIMIT 1;

--5. What is the total revenue for each product sold in January?

SELECT Product AS Product,
ROUND(SUM(quantity*price), 2) AS revenue
FROM BIT_DB.JanSales
WHERE LENGTH(price) > 0
AND Product <> 'Product'
GROUP BY Product
ORDER BY revenue;

--6. Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue?

SELECT February.Product AS Product,
February.location AS Location,
Sum(February.quantity) AS Amount,
Sum(February.quantity*February.price) AS Revenue
FROM BIT_DB.FebSales AS February
WHERE February.location LIKE '%548 Lincoln St, Seattle, WA 98101%'
GROUP BY Product, Location;

--7. How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers?

SELECT
count(distinct cust.acctnum),
avg(quantity*price)
FROM BIT_DB.FebSales Feb
LEFT JOIN BIT_DB.customers cust
ON FEB.orderid=cust.order_id
WHERE Feb.Quantity>2
AND length(orderid) = 6
AND orderid <> 'Order ID';