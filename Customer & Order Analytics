# In this SQL, I'm querying a database with multiple tables in it to quantify statistics about customer and order data. 

-- 1. How many many unique orders were placed in January? 

SELECT COUNT(DISTINCT orderID)
FROM BIT_DB.JanSales
WHERE LENGTH(orderID) = 6;

-- 2. How many of those orders were for an iPhone?

SELECT COUNT(DISTINCT JanSales.orderID)
FROM BIT_DB.JanSales
WHERE LENGTH(orderID) = 6
AND JanSales.Product = 'iPhone';

-- 3. Select the customer account numbers for all the orders that were placed in February.

SELECT DISTINCT Cust.acctnum AS account_number
FROM BIT_DB.FebSales AS Feb
INNER JOIN BIT_DB.Customers AS Cust
ON Feb.orderID = Cust.order_id
WHERE LENGTH(Feb.orderID) = 6;

--4. Which product was the cheapest one sold in January, and what was the price?

SELECT DISTINCT BIT_DB.JanSales.Product AS Product,
BIT_DB.JanSales.Price AS Price
FROM BIT_DB.JanSales
WHERE Product <> ''
AND Product <> 'Product'
GROUP BY Product, Price
ORDER BY Price
LIMIT 1;

--5. What is the total revenue for each product sold in January?

SELECT DISTINCT Jan.Product AS Product,
SUM(Jan.Quantity)*Jan.price AS Revenue
FROM BIT_DB.JanSales AS Jan
WHERE LENGTH(Jan.orderID) = 6
GROUP BY Product
ORDER BY Revenue desc;

--6. Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue?

SELECT Feb.Product AS Product,
SUM(Feb.Quantity) AS Amount,
SUM(Feb.Quantity)*Feb.price AS Revenue
FROM BIT_DB.FebSales AS Feb
WHERE Product <> ''
AND Product <> 'Product'
AND Feb.location LIKE '%548 Lincoln St, Seattle, WA 98101%'
GROUP BY Product;

--7. How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers?

SELECT COUNT(DISTINCT Cust.acctnum),
AVG(Feb.Quantity*Feb.Price)
FROM BIT_DB.FebSales AS Feb
INNER JOIN BIT_DB.Customers AS Cust
ON Feb.orderID = Cust.order_id
WHERE Feb.Quantity > 2
AND Product <> ''
AND Product <> 'Product'
ORDER BY acctnum;

--8. List all the products sold in Los Angeles in February, and include how many of each were sold.

SELECT distinct Product,
SUM(Quantity) AS Amount
FROM BIT_DB.FebSales 
WHERE location like '%Los Angeles%'
GROUP BY Product
ORDER BY Amount;

--9. Which locations in New York received at least 3 orders in January, and how many orders did they each receive?

SELECT Jan.location AS Location,
Count(Jan.orderID) AS Orders_Per_Location
FROM BIT_DB.JanSales AS Jan
WHERE Location LIKE '%New York%'
AND Product <> ''
AND Product <> 'Product'
AND LENGTH(Jan.orderID) = 6
GROUP BY Location
HAVING Orders_Per_Location >= 3
ORDER BY Orders_Per_Location;

--10. How many of each type of headphone were sold in February?

SELECT Feb.Product AS Product,
SUM(Feb.Quantity) AS Total_Amount
FROM BIT_DB.FebSales AS Feb
WHERE Product LIKE '%Headphone%'
AND Product <> ''
AND Product <> 'Product'
AND LENGTH(Feb.orderID) = 6
GROUP BY Feb.Product
ORDER BY Total_Amount
;

SELECT sum(Quantity) as quantity,
Product
FROM BIT_DB.FebSales 
WHERE Product like '%Headphones%'
GROUP BY Product;

--11. What was the average amount spent per account in February?

SELECT ROUND(AVG(Total_Spent_Per_Customer), 2) Average_Amount_Spent_Per_Customer
FROM(
    SELECT Cust.acctnum,
    ROUND(SUM(Feb.Quantity*Feb.Price), 2) AS Total_Spent_Per_Customer
    FROM BIT_DB.FebSales AS Feb
    INNER JOIN BIT_DB.Customers AS Cust
    ON Feb.orderID = Cust.order_id
    WHERE Feb.Product <> ''
    AND Feb.Product <> 'Product'
    AND LENGTH(Feb.orderID) = 6
    GROUP BY Cust.acctnum);

--12. What was the average quantity of products purchased per account in February?

SELECT ROUND(AVG(Products_Purchased_Per_Customer), 2) Average_Products_Purchased
FROM(
    SELECT Cust.acctnum,
    ROUND(SUM(Feb.Quantity), 2) AS Products_Purchased_Per_Customer
    FROM BIT_DB.FebSales AS Feb
    INNER JOIN BIT_DB.Customers AS Cust
    ON Feb.orderID = Cust.order_id
    WHERE Feb.Product <> ''
    AND Feb.Product <> 'Product'
    AND LENGTH(Feb.orderID) = 6
    GROUP BY Cust.acctnum);

--13. Which product brought in the most revenue in January and how much revenue did it bring in total?

SELECT Jan.Product AS Product,
ROUND(SUM(Jan.Quantity*Jan.Price), 2) AS Revenue
FROM BIT_DB.JanSales AS Jan
WHERE Product <> ''
AND Product <> 'Product'
AND LENGTH(orderID) = 6
GROUP BY Product
ORDER BY Revenue desc
LIMIT 1;
